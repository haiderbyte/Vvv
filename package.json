{
  "name": "existential-journal",
  "version": "1.0.0",
  "description": "Android Native App with Kotlin, Jetpack Compose and Room",
  "scripts": {
    "build": "sh gradlew assembleDebug",
    "test": "sh gradlew test"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
